server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IMENDOZA21/Online-store.git
          searchPaths: config-data
          username: ${GIT_USER:IMENDOZA21}
          password: ${GIT_PASSWORD:bbYtsIJB:v1}
          default-label: master
        default-label: master

  security:
    user:
      name: root
      password: s3cr3t
